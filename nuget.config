<?xml version="1.0" encoding="utf-8"?>
<!-- No remote package sources: this solution depends only on the .NET runtime,
     which ships inside the SDK. Restore therefore needs no network. -->
<configuration>
  <packageSources>
    <clear />
  </packageSources>
</configuration>
